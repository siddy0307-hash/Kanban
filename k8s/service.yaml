apiVersion: v1
kind: Service
metadata:
  name: kanban-frontend
  labels:
    app: kanban-frontend
spec:
  type: ClusterIP
  selector:
    app: kanban-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
